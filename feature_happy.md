# Happy Path Feature

This file is part of the merge queue bug bash.

It introduces a small, self-contained change that does not conflict with
anything else in the repository. The purpose of this PR is to verify that a
clean, approved PR lands cleanly through the Vorflux merge queue.

- Scenario: happy path
- Expected outcome: PR lands via the merge queue after 1 approval
